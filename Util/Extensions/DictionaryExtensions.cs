using System;
using System.Collections.Generic;
using System.Text;

namespace Util.Extensions
{
	public static class DictionaryExtensions
	{
		//public static Dictionary<K,V> Merge<K,V>(this Dictionary<K, V> first, IEnumerable<T> second) where T : IDictionary{
		//	foreach (var entry in second)
		//		if(!first.ContainsKey(entry.Item1))
		//		first[entry.Item1] = entry.Item2;
		//	return first;
		//}
	}
}
